# Copy to `.env` to customize the frontend.

# Bearer token sent on every /rpc request (must match FLUXGATE_ADMIN_TOKEN).
# VITE_ADMIN_TOKEN=secret123

# Force the in-repo mock backend instead of calling the real /rpc server.
# VITE_USE_MOCK=true
